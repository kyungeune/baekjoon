#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

void merge(int* A, )

int main(void)
{
	int n;
	scanf("%d", &n);
	int* A = (int*)malloc(sizeof(int) * n);



	free(A);
	return 0;
}